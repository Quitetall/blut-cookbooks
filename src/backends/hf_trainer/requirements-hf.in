# Direct requirements for the optional Hugging Face trainer environment.
#
# Regenerate requirements-hf.lock with the command recorded in its header.
# The date cutoff is deliberate: future lock refreshes must be reviewed rather
# than silently selecting packages uploaded after this release boundary.
pip==26.1.2
transformers==4.57.6
datasets==2.21.0
accelerate==1.14.0
peft==0.19.1
bitsandbytes==0.49.2
trl==0.15.2
torch==2.11.0
safetensors==0.8.0
